class DateTimeUtil {
  static String getSince(DateTime time) {
    String text = "${time.day}/${time.month}/${time.year}";
    final now = DateTime.now();

    // final since = now.toLocal();

    // if (since < 60) {

    // }

    return "";
  }
}
